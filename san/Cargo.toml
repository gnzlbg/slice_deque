[package]
name = "san"
version = "0.1.0"
authors = ["gnzlbg <gonzalobg88@gmail.com>"]
edition = "2018"

[dependencies]
slice-deque = { path = "..", features = ["unstable", "use_std"] }

[profile.dev]
panic = 'abort'

[profile.release]
panic = 'abort'
